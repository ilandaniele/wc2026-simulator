FROM node:22-slim

WORKDIR /app

# Install dependencies first (cached layer)
COPY package.json package-lock.json* ./
RUN npm ci

# Copy source (volume-mounted in dev for hot-reload)
COPY . .

EXPOSE 5173
